export interface TimeKeeping {
    timekeepingID: number | string;
    year: number | string;   
    startTimekeeping: string;
    endTimekeeping: string;
    description: string;
    isActive: boolean;   
}